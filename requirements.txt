# imageEditor — Python dependencies (tested: Python 3.13, Windows 11, PyTorch 2.13 XPU on an Intel Arc iGPU).
#
# Install PyTorch FIRST from the index that matches your accelerator, then `pip install -r requirements.txt`:
#   Intel XPU : pip install torch torchvision --index-url https://download.pytorch.org/whl/xpu
#   CUDA 12.x : pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#   CPU       : pip install torch torchvision
torch>=2.6
torchvision>=0.21

# core
numpy>=2.0
opencv-python-headless>=4.10
pillow>=10
scipy>=1.10
tqdm
requests
pyyaml
huggingface_hub>=0.30

# model runtimes (floor_edit + object_edit)
transformers>=5.0          # SegFormer, UPerNet, Sam3Model / Sam3TrackerModel
diffusers>=0.30            # RGB->X pipeline, FreeFine (SD-1.5) pipeline
accelerate>=1.0
safetensors>=0.4
sentencepiece

# repos/MoGe (moge.model.v2)
utils3d_moge @ git+https://github.com/EasternJournalist/utils3d-moge.git@62f09d58509485564e24d5d9f6aac9ee9ebc0c37
trimesh>=4.11
matplotlib

# repos/FreeFine (src.demo.model, src.utils.*)
pytorch-lightning>=2.1
timm

# repos/lama (saicinpainting.training.trainers, predict path only)
omegaconf>=2.3
hydra-core>=1.3
kornia>=0.7
albumentations>=1.3
easydict
webdataset
scikit-image
scikit-learn
pandas
joblib
tabulate
braceexpand
